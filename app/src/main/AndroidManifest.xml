<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dumposk129.create.stories.app">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="ANDROID.PERMISSION.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.set_debug_app"/>

    <supports-screens
        android:smallScreens="false"
        android:normalScreens="false"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:resizeable="true"
        android:requiresSmallestWidthDp="600"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">


        <!-- Main Menu -->
        <activity
            android:name=".navigation_drawer.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- Read Menu -->
        <activity
            android:name=".reads.Reads"
            android:label="@string/title_activity_reads" />


        <!-- Create Menu -->
        <activity
            android:name=".create.Creates"
            android:label="@string/title_activity_creates" />
        <!-- <activity
             android:name=".create.Write"
             android:label="@string/app_name" />-->
        <activity
            android:name=".create.AudioRecord"
            android:label="@string/app_name" />
        <!--
            <activity
            android:name=".create.Character_Show"
            android:label="@string/app_name" />
        -->
        <!-- <activity
             android:name=".create.SelectBackground"
             android:label="@string/title_activity_select_background"
             android:screenOrientation="landscape">
         </activity>-->

        <!-- Quizzes Menu-->
        <activity
            android:name=".quiz.Quizzes"
            android:label="@string/title_activity_quizzes">
           <!-- <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity
            android:name=".quiz.Questions"
            android:label="@string/title_activity_question_next" />
        <activity
            android:name=".quiz.NumberOfQuestion"
            android:label="@string/title_activity_num_of_questions" />
        <activity
            android:name=".quiz.Answer"
            android:label="@string/title_activity_answer" />


        <!-- Settings -->
        <activity
            android:name=".settings.Settings"
            android:label="@string/title_activity_settings" />

    </application>

</manifest>
